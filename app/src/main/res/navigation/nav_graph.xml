<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/navigation_home">

    <fragment android:id="@+id/navigation_home"
        android:name="com.example.no_thief.home"
        android:label="Home"
        tools:layout="@layout/fragment_home"/>

    <fragment android:id="@+id/navigation_history"
        android:name="com.example.no_thief.history"
        android:label="Downloads"
        tools:layout="@layout/fragment_history"/>

    <fragment android:id="@+id/navigation_security"
        android:name="com.example.no_thief.security"
        android:label="Settings"
        tools:layout="@layout/fragment_security"/>

    <fragment android:id="@+id/navigation_profile"
        android:name="com.example.no_thief.profile"
        android:label="Profile"
        tools:layout="@layout/fragment_profile"/>

</navigation>
